## Objective
Update docs, help text, and examples so `task_move` guidance matches the fixed cross-list move behavior.

## Context
- The CLI currently exposes `task_move` / `tmv` usage, but the implementation was misleading because it reported success on a non-move.
- Once the code is fixed, the user-facing help should clearly describe what `--list` does and avoid stale examples or wording.

## Work To Be Done
1. Update command help text and any tips/examples that describe `tmv --list`.
2. Update relevant documentation pages such as `README.md` and `docs/cli/TASK_COMMANDS.md`.
3. Mention any important move constraints or verification expectations if the behavior is non-obvious.

## Deliverables
- Updated CLI help text.
- Updated documentation/examples for `task_move`.

## Acceptance Criteria
- [ ] Help output reflects the corrected move behavior.
- [ ] README/docs examples for `tmv` are accurate.
- [ ] Any stale wording that implies a generic update-based move has been removed.

## Dependencies / Blockers
- Depends on the implementation being finalized first.

## Evidence
- Required comments or links: completion comment listing the updated docs/help files.
- Completion proof: captured `--help` output and doc file references.
