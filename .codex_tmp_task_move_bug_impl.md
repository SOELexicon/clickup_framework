## Objective
Implement the actual code fix for `task_move --list` so the command moves a task's home list using the correct ClickUp API flow.

## Context
- Current command code in `clickup_framework/commands/task_commands.py` sets `updates['list']` and calls `client.update_task(...)`.
- Verified on April 3, 2026 that this prints success but leaves the task in the source list.
- A working manual call already exists as proof of feasibility:
  - `client._request('PUT', f'v3/workspaces/{workspace_id}/tasks/{task_id}/home_list/{list_id}', json={})`

## Work To Be Done
1. Add a proper client/API helper for moving a task to a new home list.
2. Update `task_move_command` to use that helper when `--list` is provided.
3. Keep `--parent` handling intact and ensure combined `--list` + `--parent` flows remain coherent.
4. Validate the post-move task state before printing final success output.

## Deliverables
- Updated client/API support for home-list moves.
- Updated `task_move_command` implementation.

## Acceptance Criteria
- [ ] The implementation uses the correct ClickUp endpoint for home-list moves.
- [ ] The command succeeds only when the task is actually in the destination list.
- [ ] Existing parent-move behavior is preserved.

## Dependencies / Blockers
- Parent task defines the full regression and docs scope.

## Evidence
- Required comments or links: comment with changed files and a passing manual verification example.
- Completion proof: before/after list IDs for at least one moved task.
