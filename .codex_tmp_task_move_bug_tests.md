## Objective
Add regression coverage so `task_move --list` cannot silently regress back to a false-success state.

## Context
- The bug was only caught by manual verification because the command printed success even though the task's list never changed.
- Tests must cover both the API call shape and the user-visible command behavior.

## Work To Be Done
1. Add or update tests for the cross-list move code path in the relevant CLI/command test modules.
2. Assert that list moves call the dedicated home-list move helper instead of generic `update_task(..., list=...)`.
3. Cover failure behavior so the command does not print success when the move API fails or the destination is not applied.

## Deliverables
- New or updated regression tests for `task_move --list`.

## Acceptance Criteria
- [ ] Tests fail against the old false-success behavior.
- [ ] Tests pass with the corrected move implementation.
- [ ] The test suite clearly covers list move semantics separately from parent re-parenting.

## Dependencies / Blockers
- Depends on the implementation approach chosen in the parent task.

## Evidence
- Required comments or links: paste the exact pytest command(s) and results.
- Completion proof: passing regression output.
